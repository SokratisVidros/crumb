To install dependencies:
```sh
bun install
```

To run:
```sh
bun run dev
```

Configure a custom port (optional):
```sh
PORT=8080 bun run dev
```

Open `http://localhost:<PORT>` (defaults to `3000`).
